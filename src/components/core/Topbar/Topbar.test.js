import React from 'react';
import ReactDOM from 'react-dom';
import Topbar from './Topbar';
import { shallow } from 'enzyme';

describe('Topbar', () => {
  it('should render wihtout any crash', () => {
    expect(true).toBeTruthy();
  });
});
